小明设计了一个非常简单的文本编辑器，这个编辑器的内容S最开始是一个空字符串，另外只能通过特定的命令Q来修改这个编辑器的内容，目前支持了如下四个命令：

1 W 就是把W这个字符串追加到S的末尾

2 k 就是删除S最后的k个字符

3 k 就是输出S的第k个字符

4 回滚上一步对S的操作 （只有命令 1 和 2 会修改S，所以回滚的是 1 和 2 对S的操作）

输入描述：
第一行是一个整数 N 表示接下来会有多少个命令要在编辑器里面执行
接下来的每一行是一个需要执行的命令（如上面问题描述所给的示例）

约束条件
1. 1 <= N <= 10^6
2. 1 <= k <= S长度
3. 所有输入的内容都是ASCII
4. S 的最大长度不超过1M

输出描述：
第3个命令输出的内容

示例1 输入输出示例仅供调试，后台判断数据一般不包含示例
输入
8
1 abc
3 3
2 3
1 xy
3 2
4
4
3 1

输出
c
y
a

说明
第一行执行完后字符串为：abc
第二行输出第3个  答案为c
第三行执行完后字符串为：（空字符串）
第四行执行完后字符串为：  xy
第五行输出第2个  答案为y
第六行回滚，回滚的是第四行的操作，执行完后字符串为：（空字符串）
第七行回滚，回滚的是第三行的操作，执行完后字符串为：  abc
第八行输出第1个  答案为a
